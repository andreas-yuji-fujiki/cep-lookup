import styled from "styled-components";

export const AppContainer = styled.div `
    margin: 0 auto;
    width: 650px;
    height: 95vh;
    padding: 35px;

    display: flex;
    align-items: center;
`